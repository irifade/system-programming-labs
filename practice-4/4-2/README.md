# Практика 4. Задание 4-2: Отображение файлов в память mmap()
Программа копирует содержимое одного файла в другой, отображая их напрямую в память процесса (Вариант 4).
## Сборка и запуск
```bash
gcc -Wall -Wextra -o task task.c
./task source.txt copy.txt
```
